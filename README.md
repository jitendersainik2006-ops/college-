# PlanForge

A polished planning website with a small built-in backend.

## Run Without Backend

Open `index.html` directly in your browser.

## Run With Backend

```bash
npm start
```

Then open:

```text
http://localhost:3000
```

The backend stores planner tasks and contact requests in `data/db.json`.
